# Base URL of the backend API (used by src/api/client.ts).
# Copy this file to .env for local development. In production/Netlify,
# override VITE_API_URL in the Netlify dashboard.
VITE_API_URL=http://localhost:5000/api